import 'package:flutter/material.dart';

/// Aura palette for a campus events product:
/// violet glow, teal energy, warm sunset accents on ink / mist surfaces.
class AppColors {
  AppColors._();

  static const violet = Color(0xFF7C6CF0);
  static const violetSoft = Color(0xFFC4B5FD);
  static const teal = Color(0xFF2DD4BF);
  static const sunset = Color(0xFFFF9B73);
  static const ink = Color(0xFF0B0B14);
  static const mist = Color(0xFFF6F3FF);
}
